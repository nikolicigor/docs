---
title: "TypeScript SDK"
description: "Resource clients and typed responses"
---

## Resources and methods

<Note>
For endpoint details and payload schemas, see the API Reference.
</Note>

<AccordionGroup>
  <Accordion icon="server" title="pipelineRuns">
    - `list(params)` – filter by status, client, pipeline, paginate
    - `retrieve(id)` – get a single run
    - `getData({ pipelineRunId, offset?, limit? })` – retrieve processed rows
    - `ack({ pipelineRunId })` – mark as processed
    - `reprocess({ pipelineRunId })` – retry processing
    - `getOriginalFileUrl({ pipelineRunId, expiresIn? })`
    - `getProcessedFileUrl({ pipelineRunId, expiresIn? })`
  </Accordion>

  <Accordion icon="users" title="clients">
    - `list()` · `retrieve(id)` · `create(params)` · `update(id, params)` · `remove(id)` · `testConnection(id)`
  </Accordion>

  <Accordion icon="database" title="schemas">
    - `list()` · `retrieve(id)` · `create(params)` · `update(id, params)` · `remove(id)` · `validate({ schemaId, data })`
  </Accordion>

  <Accordion icon="shuffle" title="pipelines">
    - `list()` · `retrieve(id)` · `create(params)` · `update(id, params)` · `remove(id)` · `toggleActive(id)`
  </Accordion>

  <Accordion icon="bell" title="webhooks">
    - `list()` · `retrieve(id)` · `create(params)` · `update(id, params)` · `remove(id)` · `listDeliveries(params)`
  </Accordion>
</AccordionGroup>

## Example: Paginate Data

```ts
const runs = await filefeed.pipelineRuns.list({ status: 'completed', limit: 50 });
for (const run of runs.data) {
  let offset: number | null = 0;
  do {
    const page = await filefeed.pipelineRuns.getData({ pipelineRunId: run.id, limit: 1000, offset });
    // process page.data
    offset = page.data.length === 1000 ? (offset ?? 0) + page.data.length : null;
  } while (offset !== null);
}
```
